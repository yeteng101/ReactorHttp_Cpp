# 旧电脑数据目录：改成你想存文件的盘符/目录（Windows 用正斜杠）
# 例如把网盘文件放在 D 盘：
NETDISK_DATA_DIR=D:/AuroraDrive/data
NETDISK_USERS_DIR=D:/AuroraDrive/conf

# frp 中转参数在 frpc-home.toml 里改（frp 配置不支持环境变量）
# 域名参数在中继机的 docker-compose.relay.yml / .env 里改
